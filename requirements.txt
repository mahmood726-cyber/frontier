# Pinned to the versions the pipeline and tests were validated against.
# NOTE: pymc requires a working C/C++ toolchain (pytensor compiles the model).
# On Windows install a compiler (e.g. `conda install m2w64-toolchain` or
# `conda install gxx`); without it run_svgd_inference cannot compile.
numpy==2.4.6
pandas==2.3.3
scipy==1.17.1
pymc==6.0.1
arviz==1.1.0
pytest==9.0.3
